// Package seasons owns season-level business logic.
// SeasonService owns the full lifecycle: activation, checklist, previous-season
// lookup, roster eligibility, and active-season lookup by league.
package seasons
