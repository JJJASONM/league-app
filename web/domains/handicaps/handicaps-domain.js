// Entry point for the Handicaps domain.
// Importing this module registers the <handicap-review> custom element.
// app.js interacts with the element via DOM: element.loadSeason(id) / element.reset().

import './handicap-review-component.js';
